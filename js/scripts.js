// Optional: add functionality to restart the animation if needed
const marquee = document.querySelector('.marquee');

marquee.addEventListener('animationiteration', () => {
    // You can add any code here to reset or modify animation on each iteration if needed
});
